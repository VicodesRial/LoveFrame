# LoveFrame starts after labwc has created the graphical desktop session.
# The installer adds the command below only once and backs up an existing file.
"/home/vic/LoveFrame/scripts/run_pi.sh" &

# To stop LoveFrame temporarily without disabling future login startup:
# pkill -TERM -f "python3 -m app.main"
